   # 应用配置
   NODE_ENV=production
   PORT=6016
   
   # 数据库配置
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=
   DB_USER=
   DB_PASSWORD=
   
   # JWT配置
   JWT_SECRET=change_this_to_a_secure_secret
   JWT_EXPIRES_IN=30d
   
   # 文件上传配置
   MAX_FILE_SIZE=20
   MAX_ZIP_SIZE=500
   
   # CORS配置 (逗号分隔的URL列表)
   ALLOWED_ORIGINS=http://localhost:6017,http://127.0.0.1:6017,http://your-domain:6017